'use strict';

// 引入核心类
const exmail = require('./exmail');

// 将exmail挂载到app上
module.exports = app => {
  app.addSingleton('exmail', init);
};

/**
 * 实例化邮箱类
 * @param config
 * @return {*}
 */
function init(config) {
  return new exmail(config);
}
