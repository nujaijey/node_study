'use strict';

const mock = require('egg-mock');

describe('test/exmail.test.js', () => {
  let app;
  before(() => {
    app = mock.app({
      baseDir: 'apps/exmail-test',
    });
    return app.ready();
  });

  after(() => app.close());
  afterEach(mock.restore);

  it('should GET /', () => {
    return app.httpRequest()
      .get('/')
      .expect('hi, exmail')
      .expect(200);
  });
});
